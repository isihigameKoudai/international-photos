import { css } from "@emotion/css";

export const style = css`
  .inner {
    padding: 60px 0;
  }

  .inner-flex {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 20px;
  }
`;
